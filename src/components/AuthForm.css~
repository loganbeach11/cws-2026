.auth-form-container {
    padding: 30px;
    width: 100%;
  max-width: 400px;
  border: 2px solid #ccc;
  border-radius: 10px;
  background-color: #f9f9f9;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
  text-align: center;
}

.auth-form-container h2 {
  text-align: center;
  margin-bottom: 15px;
  margin-top: -5px; 
  color: #333;
  font-size: 25px;
}

.auth-form-container input[type="text"],
.auth-form-container input[type="password"] {
  width: 100%;
  padding: 16px 18px;
  margin-bottom: 20px;
  border: 1px solid #ccc;
  border-radius: 6px;
  box-sizing: border-box;
  font-size: 1.1rem;   
}

.auth-form-container button {
  width: 100%;
  padding: 17px;
  background-color: #0057d9;
  color: white;
  border: none;
  border-radius: 6px;
  font-size: 18.5px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.auth-form-container button:hover {
  background-color: #0041a8;
}

.auth-form-container p {
  margin-top: 15px;
  text-align: center;
  font-size: 16.5px;
}

.auth-form-container span {
  color: #0057d9;
  text-decoration: underline;
  cursor: pointer;
  transition: color 0.2s ease;
}
.auth-form-container span:hover {
  color: #003c9e; /* Darker blue or any color you prefer */
}
.toggle-button {
  margin-top: 10px;
  color: #007bff;
  cursor: pointer;
  text-decoration: underline;
  transition: color 0.3s;
}

.toggle-button:hover {
  color: #0056b3;
}
.page-wrapper {
  background-color: navy;
  width: 100%;
  min-height: 100vh;
  padding-top: 180px;
  box-sizing: border-box;
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
  touch-action: manipulation;

  /* Default: center on desktop */
  display: flex;
  align-items: flex-start;
  justify-content: center;
}




/* Responsive layout for small screens */
@media (max-width: 600px) {
  body, html {
    overflow-x: hidden;               /* Prevent sideways scroll */
    overflow-y: auto;                /* Enable vertical scroll */
    -webkit-overflow-scrolling: touch;
    height: auto;                    /* Let content dictate height */
  }

  .auth-form-container {
    width: 85%;
    padding: 20px;
    margin: 0 auto; 
  }

  .auth-form input,
  .auth-form button {
    width: 100%;
    font-size: 1rem;
  }

  .page-wrapper {
    padding-top: 40px;
    overflow-y: auto;
    -webkit-overflow-scrolling: touch;
  }

  .champion-label {
    font-size: 1rem;
  }
}

/* Base styles */
body, html {
  margin: 0;
  padding: 0;
  width: 100%;
  height: 100%;
  background-color: navy;
}

@media (min-width: 601px) {
  body.login-lock, html.login-lock {
    overflow: hidden !important;     /* Disable scrollbars on desktop */
    height: 100vh;
  }
}


@media (max-width: 600px) {
  .page-wrapper {
    display: block;
    padding-top: 40px; /* tighter spacing for mobile */
    overflow-y: auto;
  }
  
}
